option(string) find_name(bool exists) {
    if exists {
        return "Dylan";
    }
    return empty;
}

void main() {
    option(string) name = find_name(true);
    print(name.exists());
    print(name.or("Guest"));

    option(string) missing = find_name(false);
    print(missing.exists());
    print(missing.or("Guest"));

    option(int) num = empty;
    print(num.or(0));

    # unwrap then call method
    option(string) found = find_name(true);
    string val = found.or("");
    print(val.upper());

    option(string) quiet = find_name(false);
    print(quiet.or("HELLO"));
}
